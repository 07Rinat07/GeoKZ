[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "geokz"
version = "0.2.0.dev0"
description = "Доказательная геологическая информационная система Казахстана"
readme = "README.md"
requires-python = ">=3.12,<3.14"
authors = [{ name = "Sarmuldin Rinat", email = "ura07srr@gmail.com" }]
dependencies = [
    "fastapi>=0.139,<0.140",
    "uvicorn[standard]>=0.34,<1.0",
    "SQLAlchemy[asyncio]>=2.0.51,<2.1",
    "alembic>=1.18.5,<1.19",
    "asyncpg>=0.31,<0.32",
    "GeoAlchemy2>=0.17,<1.0",
    "pydantic>=2.11,<3.0",
    "pydantic-settings>=2.8,<3.0",
    "orjson>=3.10,<4.0",
    "httpx>=0.28,<1.0",
    "pyproj>=3.7,<4.0",
    "cryptography>=45,<47",
    "packaging>=24,<27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<9.0",
    "pytest-asyncio>=0.25,<1.0",
    "ruff>=0.11,<1.0",
]
desktop = [
    "PySide6>=6.8,<7.0",
]
windows-build = [
    "pyinstaller>=6.13,<7.0",
]

[project.scripts]
geokz-desktop = "scripts.desktop:main"

[project.urls]
Repository = "https://github.com/07Rinat07/GeoKZ"

[tool.setuptools.packages.find]
include = ["app*", "scripts*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
ignore = ["B008", "E501"]
